웹 프로그래밍 4주차 (0925)

자바스크립트는 C에게 영향을 받았다.
자바스크립트는 'on'문자로 명령을 시작한다.
<href>에 자바스크립트 코드 작성이 가능하다.
다이얼로그 종류: alery, conmfirm, prompt
데이터 타입이 있다는 것은 변수를 만들 수 있다는 것이다.
지역변수는 전역변수보다 우선이다. (그 지역 내에서는)
this를 통해 전역변수를 사용할 수 있다.
매개변수(parameter)는 함수 안에서의 정의 및 사용에 나열되어 있는 변수들을 의미하고 전달 인자(argument)는 함수를 호출할 때 전달되는 실제 값을 의미한다.
앞에 'is'가 붙은 함수는 TRUE나 FALSE를 반환하는 함수이다.
DOM은 BOM에 포함되어 있다.
new 키워드를 통해 코어객체를 사용한다.
와이어프레임, 프로토타입 단어 기억하기
자바스크립트는 배열을 만들 때 대괄호([])를 사용한다.
new array()객체를 사용하기도 한다.
자바스크립트는 규칙이 없는편이다.
값들을 저장하는 테이블이 있다고 생각하라.
※ceil(): 소수점 자리 숫자 무조건올림
※floor(): 소수점 자리 숫자 무조건 내림
※round(): 반올림
프로토타입: new연산자로 만든다.